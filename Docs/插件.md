# 插件清单

## 第一批（当前安装）

| 插件 | 状态 | 说明 |
|---|---|---|
| `tymon/jwt-auth` | ✅ 已装 | JWT 无刷新 Token 认证 |
| `intervention/image` | ✅ 已装 | 图片处理（封面图压缩/裁剪） |
| `filament/filament` | ✅ 已装 | 管理端后台（审核工作台 + 敏感词 + 分类标签） |
> 原本：filament 未列，已有的只有 tymon/jwt-auth 和 intervention/image
> 改后：filament 已装，用于 Filament Resource 管理端
| `maatwebsite/excel` | ~~⏳ 暂缓~~ → ❌ 不再需要 | 依赖 phpoffice/phpspreadsheet 不支持 PHP 8.5.1；且敏感词导入改为 TXT 格式，不需要 Excel |
| `barryvdh/laravel-cors` | ❌ 不装 | Laravel 11+ 已内置 CORS 中间件，无需单独安装 |

安装后配置：

### tymon/jwt-auth
- `config/jwt.php` 已发布
- `JWT_SECRET` 已写入 `.env`
- 下一步：`User` Model 实现 `JWTSubject` 接口，`auth.php` guard 改为 jwt

### intervention/image
- 无需额外配置，默认驱动 GD

---

## 第二批（业务跑通后再装）

| 插件 | 用途 |
|---|---|
| `spatie/laravel-permission` | ❌ 不再需要 | 管理员权限判断直接用 `role` 字段 + 中间件，无需装此插件 |
| `laravel/scout` + `meilisearch/meilisearch-php` | 全文搜索（文章标题+作者名） |

---

## 暂不装（后续按需）

| 插件 | 用途 |
|---|---|
| `barryvdh/laravel-dompdf` | 导出 PDF |
| `overtrue/laravel-lang` | 中文本地化 |
| `mews/captcha` | 图形验证码 |
